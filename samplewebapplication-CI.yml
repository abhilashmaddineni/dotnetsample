resources:
      repositories:
        - repository: templates
          type: git
          name: SarDevops/templates
    
    stages:
      - template: Templates/dotnet-build.yml@templates
        parameters: 
          branch: dev
